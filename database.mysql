CREATE TABLE IF NOT EXISTS user_db (
    Username varchar(15),
    Password varchar(20),
    Last_name varchar(20),
    First_name varchar(20),
    email varchar(20),
    phone varchar(10),
    authorization_level varchar(10),
	PRIMARY KEY (Username)   	
)